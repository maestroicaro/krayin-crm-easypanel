# ============================================
# Krayin CRM - EasyPanel Environment Variables
# ============================================
# Este arquivo contém APENAS as variáveis essenciais para rodar no EasyPanel.
# Copie e cole no campo "Environment" do seu serviço no EasyPanel.

# ============================================
# Application Settings
# ============================================
APP_NAME='Krayin CRM'
APP_ENV=production
APP_KEY=base64:GERAR_AUTOMATICAMENTE_NO_PRIMEIRO_DEPLOY
APP_DEBUG=false
APP_URL=https://seu-dominio.easypanel.host
APP_TIMEZONE=UTC
APP_LOCALE=pt_BR
APP_CURRENCY=BRL

# ============================================
# Logging
# ============================================
LOG_CHANNEL=stderr
LOG_LEVEL=info

# ============================================
# Database (MySQL)
# ============================================
# IMPORTANTE: DB_HOST deve ser o nome do serviço MySQL no EasyPanel
# Formato: projeto_nome-do-servico-mysql
DB_CONNECTION=mysql
DB_HOST=projeto01_projeto01_mysql
DB_PORT=3306
DB_DATABASE=krayin
DB_USERNAME=krayin
DB_PASSWORD=SENHA_GERADA_PELO_EASYPANEL

# ============================================
# Cache, Queue & Session (Redis)
# ============================================
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120

# ============================================
# Redis Connection
# ============================================
# IMPORTANTE: REDIS_HOST deve ser o nome do serviço Redis no EasyPanel
# REDIS_PASSWORD deve ser a senha do serviço Redis (copie do painel)
REDIS_HOST=redis-krayin
REDIS_PASSWORD=SENHA_DO_REDIS_NO_EASYPANEL
REDIS_PORT=6379

# ============================================
# Email Configuration (Opcional)
# ============================================
# Configure apenas se for usar envio de emails (recuperação de senha, notificações)
# Para desenvolvimento, deixe como está (mailhog)
# Para produção, use um serviço SMTP real (Gmail, SendGrid, etc)
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=noreply@seudominio.com
MAIL_FROM_NAME="${APP_NAME}"

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. APP_KEY: Será gerado automaticamente no primeiro deploy pelo entrypoint.sh
#    Se precisar gerar manualmente: php artisan key:generate --show
#
# 2. DB_HOST: Use o formato "projeto_nome-do-servico-mysql" 
#    Exemplo: projeto01_projeto01_mysql
#
# 3. REDIS_HOST: Nome do serviço Redis no EasyPanel
#    Exemplo: redis-krayin
#
# 4. REDIS_PASSWORD: Copie da aba de configuração do serviço Redis
#
# 5. APP_URL: Use o domínio HTTPS completo fornecido pelo EasyPanel
#    Exemplo: https://projeto01-krayin.ynbvqv.easypanel.host
#
# 6. Para configurar email em produção, substitua:
#    MAIL_HOST=smtp.gmail.com (ou seu provedor)
#    MAIL_PORT=587
#    MAIL_USERNAME=seu-email@gmail.com
#    MAIL_PASSWORD=sua-senha-de-app
#    MAIL_ENCRYPTION=tls
